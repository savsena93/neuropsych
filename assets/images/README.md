Drop your real, licensed stimulus image files here (PNG/JPG), then
reference them from a content file in `data/` using a relative path,
e.g.:

    stimulusImage: 'assets/images/moca-naming-animal-1.png'

Keep every reference a local relative path like the one above — never
a remote URL — so the app keeps working with no internet connection.
